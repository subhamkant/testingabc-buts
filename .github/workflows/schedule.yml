name: Mahabharata YouTube Bot

on:
  schedule:
    # Mahabharata Hindi — 2x/day at optimal IST evening windows.
    # Re-enabled 2026-05-18 after the manual #4 re-render work concluded.
    # Run 1 ->  8:30 PM IST (15:00 UTC) — prime evening audience
    - cron: "0 15 * * *"
    # Run 2 -> 11:30 PM IST (18:00 UTC) — late evening, second peak
    - cron: "0 18 * * *"

  workflow_dispatch:
    inputs:
      target:
        description: "What to generate (hi / en / whatif / krishna)"
        required: true
        default: "hi"
        type: choice
        options:
          - hi
          - en
          - whatif
          - krishna
      episode_number:
        description: "Force episode number in title (e.g. '4' for re-rendering blocked #4). Leave blank for auto."
        required: false
        default: ""
        type: string
      skip_instagram:
        description: "Skip Instagram cross-post (YouTube-only run)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

# ════════════════════════════════════════════════════════════════════════════
# WORKFLOW STRUCTURE
# ════════════════════════════════════════════════════════════════════════════
# Every job has `timeout-minutes: 29` so it fits inside a single 30-min GHA
# slot. To absorb runs that genuinely need longer (Mahabharata's ~80 min slow
# path when AI clips are exhausted), each primary has THREE chained retry
# siblings (retry-1, retry-2, retry-3). Each retry fires only if the prior
# attempt was `failure` or `cancelled` (timeout-cancellation counts as
# `cancelled`, not `failure`) and resumes from the shared checkpoint cache.
#
# Total budget per series: 4 attempts × 29 min = 116 min — covers the slowest
# observed runs with margin while staying under the 30-min-per-attempt cap.
#
# Series → primary job mapping:
#   - Mahabharata + Krishna: generate-and-upload  (cron picks which target)
#   - WhatIf English phase:  whatif-english       (only on 04:00 UTC cron)
#   - WhatIf Hindi phase:    whatif-hindi         (depends on EN succeeding)
#
# All retry jobs in a series share ONE artifact name — pipeline-checkpoint-
# <PIPELINE_RUN_ID> — with `overwrite: true`, so each attempt's partial state
# overwrites the prior and the next attempt resumes from wherever the last
# one left off.

jobs:
  # ════════════════════════════════════════════════════════════════════════
  # Mahabharata + Krishna — single-job-family pipeline (4 attempts)
  # ════════════════════════════════════════════════════════════════════════

  generate-and-upload:
    # Skip on the WhatIf cron + on workflow_dispatch with target=whatif.
    if: |
      github.event.schedule != '0 4 * * *' &&
      !(github.event_name == 'workflow_dispatch' && inputs.target == 'whatif')
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - name: Determine target + PIPELINE_RUN_ID
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "TARGET=${{ inputs.target }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 13 * * *" ]; then
            echo "TARGET=krishna" >> $GITHUB_ENV
          else
            echo "TARGET=hi" >> $GITHUB_ENV
          fi
          # Resolve series for the cache key — both 'hi' and 'en' map to mahabharata.
          T="$TARGET"
          [ -z "$T" ] && T="${{ inputs.target }}"
          SERIES="$T"
          [ "$T" = "hi" ] || [ "$T" = "en" ] && SERIES="mahabharata"
          echo "PIPELINE_RUN_ID=${SERIES}_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py ${{ env.TARGET }}"
          commit-message: "Auto-update pipeline state [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  generate-and-upload-retry-1:
    needs: generate-and-upload
    # Fire if primary was cancelled (timeout) OR failed. Skip if it succeeded
    # OR was skipped (cron didn't route here).
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.generate-and-upload.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - name: Determine target + PIPELINE_RUN_ID (same as primary)
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "TARGET=${{ inputs.target }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 13 * * *" ]; then
            echo "TARGET=krishna" >> $GITHUB_ENV
          else
            echo "TARGET=hi" >> $GITHUB_ENV
          fi
          T="$TARGET"
          [ -z "$T" ] && T="${{ inputs.target }}"
          SERIES="$T"
          [ "$T" = "hi" ] || [ "$T" = "en" ] && SERIES="mahabharata"
          echo "PIPELINE_RUN_ID=${SERIES}_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py ${{ env.TARGET }}"
          commit-message: "Auto-update pipeline state (retry-1) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  generate-and-upload-retry-2:
    needs: generate-and-upload-retry-1
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.generate-and-upload-retry-1.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - name: Determine target + PIPELINE_RUN_ID (same as primary)
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "TARGET=${{ inputs.target }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 13 * * *" ]; then
            echo "TARGET=krishna" >> $GITHUB_ENV
          else
            echo "TARGET=hi" >> $GITHUB_ENV
          fi
          T="$TARGET"
          [ -z "$T" ] && T="${{ inputs.target }}"
          SERIES="$T"
          [ "$T" = "hi" ] || [ "$T" = "en" ] && SERIES="mahabharata"
          echo "PIPELINE_RUN_ID=${SERIES}_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py ${{ env.TARGET }}"
          commit-message: "Auto-update pipeline state (retry-2) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  generate-and-upload-retry-3:
    needs: generate-and-upload-retry-2
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.generate-and-upload-retry-2.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - name: Determine target + PIPELINE_RUN_ID (same as primary)
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "TARGET=${{ inputs.target }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 13 * * *" ]; then
            echo "TARGET=krishna" >> $GITHUB_ENV
          else
            echo "TARGET=hi" >> $GITHUB_ENV
          fi
          T="$TARGET"
          [ -z "$T" ] && T="${{ inputs.target }}"
          SERIES="$T"
          [ "$T" = "hi" ] || [ "$T" = "en" ] && SERIES="mahabharata"
          echo "PIPELINE_RUN_ID=${SERIES}_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py ${{ env.TARGET }}"
          commit-message: "Auto-update pipeline state (retry-3) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  # ════════════════════════════════════════════════════════════════════════
  # WhatIf English phase (4 attempts) — runs on 04:00 UTC cron
  # ════════════════════════════════════════════════════════════════════════

  whatif-english:
    if: |
      github.event.schedule == '0 4 * * *' ||
      (github.event_name == 'workflow_dispatch' && inputs.target == 'whatif')
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=en"
          commit-message: "Auto-update pipeline state (whatif-en) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-english-retry-1:
    needs: whatif-english
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-english.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=en"
          commit-message: "Auto-update pipeline state (whatif-en retry-1) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-english-retry-2:
    needs: whatif-english-retry-1
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-english-retry-1.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=en"
          commit-message: "Auto-update pipeline state (whatif-en retry-2) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-english-retry-3:
    needs: whatif-english-retry-2
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-english-retry-2.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=en"
          commit-message: "Auto-update pipeline state (whatif-en retry-3) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  # ════════════════════════════════════════════════════════════════════════
  # WhatIf Hindi phase (4 attempts) — depends on ANY EN attempt succeeding
  # ════════════════════════════════════════════════════════════════════════

  whatif-hindi:
    needs:
      - whatif-english
      - whatif-english-retry-1
      - whatif-english-retry-2
      - whatif-english-retry-3
    if: |
      always() && (
        needs.whatif-english.result == 'success' ||
        needs.whatif-english-retry-1.result == 'success' ||
        needs.whatif-english-retry-2.result == 'success' ||
        needs.whatif-english-retry-3.result == 'success'
      ) && (
        github.event.schedule == '0 4 * * *' ||
        (github.event_name == 'workflow_dispatch' && inputs.target == 'whatif')
      )
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=hi"
          commit-message: "Auto-update pipeline state (whatif-hi) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-hindi-retry-1:
    needs: whatif-hindi
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-hindi.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=hi"
          commit-message: "Auto-update pipeline state (whatif-hi retry-1) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-hindi-retry-2:
    needs: whatif-hindi-retry-1
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-hindi-retry-1.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=hi"
          commit-message: "Auto-update pipeline state (whatif-hi retry-2) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}

  whatif-hindi-retry-3:
    needs: whatif-hindi-retry-2
    if: |
      always() &&
      contains(fromJson('["failure", "cancelled"]'), needs.whatif-hindi-retry-2.result)
    runs-on: ubuntu-latest
    timeout-minutes: 29
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pipeline-runner
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          youtube-token-b64: ${{ secrets.YOUTUBE_TOKEN_B64 }}
          youtube-client-secrets: ${{ secrets.YOUTUBE_CLIENT_SECRETS }}
      - run: echo "PIPELINE_RUN_ID=whatif_${{ github.run_id }}" >> $GITHUB_ENV
      - uses: ./.github/actions/run-pipeline-with-checkpoint
        with:
          pipeline-run-id: ${{ env.PIPELINE_RUN_ID }}
          pipeline-cmd: "python main.py whatif --lang=hi"
          commit-message: "Auto-update pipeline state (whatif-hi retry-3) [skip ci]"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          gemini-api-key-fallback: ${{ secrets.GEMINI_API_KEY_FALLBACK }}
          gemini-api-key-fallback-2: ${{ secrets.GEMINI_API_KEY_FALLBACK_2 }}
          gemini-api-key-fallback-3: ${{ secrets.GEMINI_API_KEY_FALLBACK_3 }}
          instagram-access-token: ${{ secrets.IG_ACCESS_TOKEN }}
          instagram-business-account-id: ${{ secrets.IG_BUSINESS_ACCOUNT_ID }}
          episode-number-override: ${{ inputs.episode_number || '' }}
          skip-instagram: ${{ inputs.skip_instagram || 'false' }}
          groq-api-key: ${{ secrets.GROQ_API_KEY }}
          fal-key: ${{ secrets.FAL_KEY }}
          replicate-api-token: ${{ secrets.REPLICATE_API_TOKEN }}
          hf-token: ${{ secrets.HF_TOKEN }}
          hf-space: ${{ secrets.HF_SPACE }}
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare-api-token-2: ${{ secrets.CLOUDFLARE_API_TOKEN_2 }}
          cloudflare-account-id-2: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_2 }}
          cloudflare-api-token-3: ${{ secrets.CLOUDFLARE_API_TOKEN_3 }}
          cloudflare-account-id-3: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_3 }}
          elevenlabs-api-key: ${{ secrets.ELEVENLABS_API_KEY }}
          elevenlabs-api-key-fallback: ${{ secrets.ELEVENLABS_API_KEY_FALLBACK }}
          elevenlabs-voice-id: ${{ secrets.ELEVENLABS_VOICE_ID }}
          elevenlabs-voice-id-krishna: ${{ secrets.ELEVENLABS_VOICE_ID_KRISHNA }}
