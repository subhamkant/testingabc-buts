# Manual topic queue — first non-comment line gets consumed by the next
# Mahabharata cron run and removed after use.
#
# Currently empty: cron picks the next unused Bhishma-arc topic via
# _pick_next_arc_topic() — Shikhandi #5 is next based on recent_topics.json.
# Re-add a topic line above this comment block only when you want to
# force-render a specific topic via cron / workflow_dispatch.
