# Manual topic queue — first non-comment line gets consumed by the next
# Mahabharata cron run and removed after use.
#
# 2026-05-17: re-rendering #4 Bhishma Kurukshetra via GHA workflow_dispatch
# (local IP keeps tripping CF's per-IP rate limit). Trigger inputs:
#   target = hi
#   episode_number = 4
#   skip_instagram = true
# After this consumes the line, the queue will be empty until tomorrow's
# re-enable of cron + new topic queue.
